"use strict";

/**
 * Backwards-compatible entry: delegates to apply-theme-scope-utf8.js
 * so styles.css stays UTF-8 (CJK font names must not be corrupted on Windows).
 */
require("./apply-theme-scope-utf8.js");
