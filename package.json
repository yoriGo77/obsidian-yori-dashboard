{
  "name": "yori-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Development tooling for Yori Dashboard plugin.",
  "scripts": {
    "build": "node scripts/bundle.js",
    "test": "node --test tests/*.test.js"
  }
}
